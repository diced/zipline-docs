# zipline-docs/build-tools

some utilities and tools for the Zipline docs.

- `sidebar-gen` - generates a sidebar.json that is used by the sidebar component
- `validate-links` - validates all links in mdx files to make sure that they aren't dead links
