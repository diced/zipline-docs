# Models

## Reference

- If a type has `?` in its definiition, it means that the field can be `null`.
- If a type has `~` in its definition, it means that the field can be `undefined` (may not exist for certain requests!).
