# API

API Documentation is a work-in-progress. The API is always subject to change, so please refer to the [`src/server/routes`](https://github.com/diced/zipline/tree/trunk/src/server/routes) directory for the source code of the API routes.

## Models

For the sake of simplicity, the API documentation will use "models" to refer to certain data structures represented in the database and API.

To see all models see the [models page](/docs/api/models).
